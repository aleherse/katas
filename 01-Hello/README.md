# Hello World!

The well known Hello world! example using Behat

    cd behat
    composer install
    bin/behat

The well known Hello world! example using PHPSpec

    cd phpspec2
    composer install
    bin/phpspec run